#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <map>

using std::string;
using std::vector;
using std::map;
using std::pair;

class Progress {
public:
	map <string, vector<short>> progress;

string ToString() {}
string ToFile() {}
};